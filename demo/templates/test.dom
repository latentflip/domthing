<div>
    <input type="checkbox" checked="{{foo}}">

    <div class='{{ (concat "foo" " ") }}'></div>

    {{#if (not (not foo))}}
        <a>hi</a>
    {{#else}}
        <b>there</b>
    {{/if}}
</div>
